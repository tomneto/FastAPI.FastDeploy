{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn main:app --host '0.0.0.0' --port 9873",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/main.py"
    }
  ]
}