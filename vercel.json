{
  "installCommand": "pip install -r requirements.txt",
  "devCommand": "uvicorn main:App --host '0.0.0.0' --port 9873",
  "builds": [
    {
      "src": "*.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/main.py"
    }
  ]
}